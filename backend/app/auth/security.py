"""
Auth security utilities — stub module retained for import compatibility.
All actual auth is now handled by Supabase Auth.
Password hashing and JWT signing are no longer done by the backend.
"""
# This module is intentionally minimal.
# Supabase Auth handles token signing, password hashing, and verification.
# The old python-jose / passlib / bcrypt dependencies have been removed.

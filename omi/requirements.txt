# Omi Integration Layer — Python dependencies
# Install: pip install -r omi/requirements.txt

# ── Core HTTP ─────────────────────────────────────────────────────────────────
httpx==0.27.0
tenacity==8.3.0

# ── Settings & Validation ─────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# ── Encryption (temp audio buffer) ───────────────────────────────────────────
cryptography==47.0.0

# ── Acoustic Analysis ─────────────────────────────────────────────────────────
librosa==0.10.2
numpy==1.26.4
soundfile==0.12.1
scipy==1.13.0

# ── Transcription Fallback ────────────────────────────────────────────────────
openai==2.33.0               # Whisper API via OpenAI SDK (also used for Whisper API calls)
# openai-whisper==20231117   # Local Whisper model (optional — uncomment for local inference)

# ── Language Detection ────────────────────────────────────────────────────────
langdetect==1.0.9

# ── Bluetooth (optional — BLE scanning) ──────────────────────────────────────
# bleak==0.22.0              # Uncomment if running on a machine with Bluetooth hardware

# ── Logging ───────────────────────────────────────────────────────────────────
structlog==24.2.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-mock==3.14.0
pytest-cov==5.0.0
factory-boy==3.3.0
